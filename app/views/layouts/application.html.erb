<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>
  <body>
    <%= render 'shared/navbar' if @should_render_navbar %>
    <%= render 'shared/notices' %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>